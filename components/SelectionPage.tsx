'use client';
import Link from 'next/link'; import {Trash2, MessageCircle} from 'lucide-react'; import Header from './Header'; import {formatPrice} from './data'; import {useSelection} from './SelectionProvider'; import { whatsappPhone } from '../lib/site';
export default function SelectionPage(){const {selected,toggle,clear}=useSelection();const total=selected.reduce((a,p)=>a+p.price,0);const lines=selected.map(p=>`• ${p.title} — ${formatPrice(p.price)}`).join('%0A');return <><Header/><main className="selection-page"><p className="eyebrow">Tu selección</p><h1>Me interesan<br/><em>varias cosas.</em></h1>{selected.length?<><div className="selection-list">{selected.map(p=><div className="selected-item" key={p.id}><img src={p.image} alt=""/><div><b>{p.title}</b><span>{formatPrice(p.price)}</span></div><button onClick={()=>toggle(p)} aria-label={'Quitar '+p.title}><Trash2 size={17}/></button></div>)}</div><div className="selection-total"><span>Total publicado</span><strong>{formatPrice(total)}</strong></div><div className="selection-actions"><a href={`https://wa.me/${whatsappPhone}?text=Hola%2C%20me%20interesan%20estas%20cosas%3A%0A${lines}%0A%0A%C2%BFSiguen%20disponibles%3F`} className="primary-action"><MessageCircle size={18}/> Consultar selección</a><a href={`https://wa.me/${whatsappPhone}?text=Hola%2C%20quiero%20hacer%20una%20oferta%20por%3A%0A${lines}%0A%0AMi%20oferta%20es%3A%20%24`} className="offer-link">¿Te sirve hacer una oferta?</a><button className="clear-link" onClick={clear}>Vaciar selección</button></div></>:<div className="selection-empty"><div>✦</div><h2>Acá van tus cosas favoritas.</h2><p>Guardá objetos mientras recorrés el catálogo. Esta selección vive solo en este navegador.</p><Link href="/">Ver el catálogo →</Link></div>}</main></>}
