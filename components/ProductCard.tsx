'use client';
import Link from 'next/link'; import { Check, Plus } from 'lucide-react'; import { Product, formatPrice, statusLabel } from './data'; import { useSelection } from './SelectionProvider';
export default function ProductCard({product}:{product:Product}) { const {toggle,has}=useSelection(); const saved=has(product.id); return <article className="product-card"><Link href={'/producto/'+product.id} className="product-image"><img src={product.image} alt=""/><span className={'status '+product.status}>{statusLabel[product.status]}</span></Link><div className="product-copy"><div><p className="category">{product.category}</p><Link href={'/producto/'+product.id}><h3>{product.title}</h3></Link><strong>{formatPrice(product.price)}</strong></div>{product.status==='available'&&<button className={'add-button '+(saved?'saved':'')} onClick={()=>toggle(product)} aria-label="Agregar a selección">{saved?<Check size={19}/>:<Plus size={20}/>}</button>}</div></article> }
