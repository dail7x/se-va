import Link from 'next/link'; import {products,formatPrice,statusLabel} from '../../components/data';
export default function Admin(){const available=products.filter(p=>p.status==='available').length;return <main className="admin"><header className="admin-header"><Link className="logo" href="/">SE VA<span>!</span></Link><span>Panel de casa</span><Link href="/">Ver catálogo →</Link></header><div className="admin-inner"><div className="admin-title"><div><p className="eyebrow">Mi inventario</p><h1>Todo en su lugar.</h1></div><button className="primary-action">+ Nueva cosa</button></div><div className="metrics"><div><b>{products.length}</b><span>Cosas en total</span></div><div><b>{available}</b><span>Todavía están</span></div><div><b>{products.filter(p=>p.status==='reserved').length}</b><span>Casi se van</span></div><div><b>{products.filter(p=>p.status==='sold').length}</b><span>Se fueron</span></div></div><div className="admin-table"><div className="table-head"><span>Cosa</span><span>Estado</span><span>Precio</span><span>Acción</span></div>{products.map(p=><div className="table-row" key={p.id}><div className="admin-product"><img src={p.image} alt=""/><b>{p.title}</b></div><span className={'status '+p.status}>{statusLabel[p.status]}</span><span>{formatPrice(p.price)}</span><Link href={'/producto/'+p.id}>Ver →</Link></div>)}</div></div></main>}
